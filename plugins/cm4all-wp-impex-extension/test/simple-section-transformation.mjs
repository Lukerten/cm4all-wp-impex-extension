import test from 'node:test';
import assert from 'node:assert';
import transform from '../src/index.mjs';

/*
 
test('basic section transform', (t) => {
  const source = [
    {
      elType: 'section',
      settings: [],
      elements: [],
    },
  ];

  assert.deepEqual(transform(source, {foo: 'bar'}), [
    {
      attributes: {},
      innerBlocks: [],
      type: 'core/paragraph',
    },
  ]);
});

*/
